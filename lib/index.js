// var generate = require('./src/generate');

module.exports = (api) => {
  // console.log('📁 pod plugin added');
  // generate
  // console.log('xxx', api.registerCommand)
  // ['generate', 'g'].forEach((command) => {
    // api.registerCommand(command, {
    //   'description': 'Create pod structure component',
    //   'usage': 'vue-cli-service generate or g [options]',
    // },(args) => generate(args))
  // });


//   api.registerCommand('delete', {
//     'description': 'Create pod structure component',
//     'usage': 'vue-cli-service delete <componentname>'
//   },(args) => {
//     console.log('called', args)
//   })
}
