const chalk = require('chalk');
var utils = require('./utils');

module.exports = (args) => {
  console.log(args)
  // let { file, config } = utils.getConfig();
  // let msg = (file === 'default') ? 'Using default config' : `Using config file ${file}`;
  // chalk.blue(msg);

  // console.log(args)
}
